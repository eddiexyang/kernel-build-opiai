if (${PRODUCT} STREQUAL as31xm1)
    add_subdirectory(hdc_host)
    add_subdirectory(hdc_slave)
else ()
    if (${PRODUCT_SIDE} STREQUAL host)
    add_subdirectory(hdc_host)
    add_subdirectory(vhdc)
    else ()
    add_subdirectory(hdc_slave)
    endif ()
endif ()