/*
 * Legacy audio sys wrapper header.
 * The restored audio sources still include this file, but within the
 * current source snapshot all consumed UMAP helpers already come from
 * dev_ext.h, so this header only preserves the include contract.
 */

#ifndef MKP_SYS_H
#define MKP_SYS_H

#endif
