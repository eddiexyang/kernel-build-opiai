.PHONY: headers-apk

headers-apk: | ensure-output-dirs
	kernel_release=
	package_name=
	package_arch=
	package_version=
	apk_workspace=
	pkg_root=
	headers_dir=
	modules_dir=
	apk_path=
	printf 'start generate headers apk (OpenWrt)\n'
	printf 'kernel release suffix: %s\n' "$(KERNEL_RELEASE_SUFFIX)"
	if [ -n "$(strip $(CCACHE_BIN))" ]; then printf 'ccache enabled: %s\n' "$(CCACHE_BIN)"; else printf 'ccache disabled\n'; fi
	$(MAKE) --no-print-directory sync-kernel-source-inputs
	$(MAKE) --no-print-directory clean-kernel-build-artifacts
	$(MAKE) -C "$(KERNEL_WORKSPACE)" $(KERNEL_MAKE_VARS) "$(KERNEL_DEFCONFIG)"
	$(MAKE) -C "$(KERNEL_WORKSPACE)" $(KERNEL_MAKE_VARS) olddefconfig
	$(MAKE) -C "$(KERNEL_WORKSPACE)" $(KERNEL_MAKE_VARS) modules_prepare
	$(MAKE) -C "$(KERNEL_WORKSPACE)" $(KERNEL_MAKE_VARS) modules -j"$(JOBS)"
	kernel_release="$$( $(MAKE) -s -C "$(KERNEL_WORKSPACE)" $(KERNEL_MAKE_VARS) kernelrelease )"
	[ -n "$$kernel_release" ] || { printf 'error: failed to determine kernel release\n' >&2; exit 1; }
	package_name="$${HEADERS_APK_PACKAGE_NAME:-linux-headers-$$kernel_release}"
	package_arch="$${HEADERS_APK_PACKAGE_ARCH:-aarch64}"
	package_version="$${HEADERS_APK_PACKAGE_VERSION:-$(BUILD_TIMESTAMP_MINUTE)}"
	apk_workspace="$(WORKSPACE_DIR)/headers-apk"
	pkg_root="$$apk_workspace/pkgroot"
	headers_dir="$$pkg_root/usr/src/$$package_name"
	modules_dir="$$pkg_root/lib/modules/$$kernel_release"
	apk_path="$(OUTPUT_DIR)/$${package_name}-$${package_version}.apk"
	rm -rf "$$apk_workspace"
	mkdir -p "$$headers_dir" "$$modules_dir"
	for path in .config Makefile Kbuild Kconfig Module.symvers System.map include scripts arch/"$(ARCH_TYPE)"; do \
		if [ -e "$(KERNEL_WORKSPACE)/$$path" ]; then \
			mkdir -p "$$headers_dir/$$(dirname "$$path")"; \
			cp -a "$(KERNEL_WORKSPACE)/$$path" "$$headers_dir/$$path"; \
		fi; \
	done
	ln -s "../../../usr/src/$$package_name" "$$modules_dir/build"
	ln -s "../../../usr/src/$$package_name" "$$modules_dir/source"
	mkdir -p "$$apk_workspace/control"
	cat > "$$apk_workspace/control/.PKGINFO" <<-PKGINFO
	# Generated by kernel-build-opiai
	pkgname = $${package_name}
	pkgver = $${package_version}-r0
	pkgdesc = Linux kernel headers for $${kernel_release}
	url = https://github.com/HwHiAiUser/kernel-build-opiai
	builddate = $$(date +%s)
	packager = kernel-build-opiai
	size = $$(du -sb "$$pkg_root" | cut -f1)
	arch = $${package_arch}
	origin = $${package_name}
	maintainer = HwHiAiUser
	PKGINFO
	sed -i 's/^\t//' "$$apk_workspace/control/.PKGINFO"
	cd "$$apk_workspace/control" && tar czf "$$apk_workspace/control.tar.gz" .PKGINFO
	cd "$$pkg_root" && tar czf "$$apk_workspace/data.tar.gz" .
	cd "$$apk_workspace" && cat control.tar.gz data.tar.gz > "$$apk_path"
	printf 'generate %s success\n' "$$apk_path"
