From c28c031a74528c66e4cf5214ca0c390a4dbae9ba Mon Sep 17 00:00:00 2001
From: yangjunyi <yangjunyi5@huawei.com>
Date: Wed, 14 Dec 2022 10:01:03 +0800
Subject: [PATCH 0865/1398] [Huawei] drivers: hooks: add igb torl/totl

Offering: HULK
starry inclusion
category: bugfix
bugzilla: 187991

----------------------------------

Signed-off-by: yangjunyi <yangjunyi5@huawei.com>
---
 drivers/net/ethernet/intel/igb/Makefile    |  1 +
 drivers/net/ethernet/intel/igb/e1000_hw.h  |  2 +
 drivers/net/ethernet/intel/igb/e1000_led.c | 54 ++++++++++++++++++++++
 drivers/net/ethernet/intel/igb/e1000_led.h | 38 +++++++++++++++
 drivers/net/ethernet/intel/igb/e1000_nvm.c |  1 +
 drivers/net/ethernet/intel/igb/e1000_phy.c |  1 +
 drivers/net/ethernet/intel/igb/igb_main.c  |  4 ++
 7 files changed, 101 insertions(+)
 create mode 100644 drivers/net/ethernet/intel/igb/e1000_led.c
 create mode 100644 drivers/net/ethernet/intel/igb/e1000_led.h

diff --git a/drivers/net/ethernet/intel/igb/Makefile b/drivers/net/ethernet/intel/igb/Makefile
index 394c1e0656b9..ce3091f03f75 100644
--- a/drivers/net/ethernet/intel/igb/Makefile
+++ b/drivers/net/ethernet/intel/igb/Makefile
@@ -9,3 +9,4 @@ obj-$(CONFIG_IGB) += igb.o
 igb-objs := igb_main.o igb_ethtool.o e1000_82575.o \
 	    e1000_mac.o e1000_nvm.o e1000_phy.o e1000_mbx.o \
 	    e1000_i210.o igb_ptp.o igb_hwmon.o
+igb-$(CONFIG_VENDOR_IGB_HOOKS) += e1000_led.o
\ No newline at end of file
diff --git a/drivers/net/ethernet/intel/igb/e1000_hw.h b/drivers/net/ethernet/intel/igb/e1000_hw.h
index 5d87957b2627..7bab92abb695 100644
--- a/drivers/net/ethernet/intel/igb/e1000_hw.h
+++ b/drivers/net/ethernet/intel/igb/e1000_hw.h
@@ -245,6 +245,8 @@ struct e1000_hw_stats {
 	u64 o2bspc;
 	u64 b2ospc;
 	u64 b2ogprc;
+	u64 torl;
+	u64 totl;
 };
 
 struct e1000_host_mng_dhcp_cookie {
diff --git a/drivers/net/ethernet/intel/igb/e1000_led.c b/drivers/net/ethernet/intel/igb/e1000_led.c
new file mode 100644
index 000000000000..68921c616c77
--- /dev/null
+++ b/drivers/net/ethernet/intel/igb/e1000_led.c
@@ -0,0 +1,54 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (c) Huawei Technologies Co., Ltd. 2022-2022. All rights reserved. */
+
+#include "e1000_led.h"
+#include "e1000_hw.h"
+#include "e1000_regs.h"
+
+s32 igb_ctrl_led_generic(struct e1000_hw *hw, s32 led_id, s32 mode)
+{
+	u32 ledctl = 0;
+	u32 shift[] = {E1000_LEDCTL_LED0_MODE_SHIFT, E1000_LEDCTL_LED1_MODE_SHIFT,
+				   E1000_LEDCTL_LED2_MODE_SHIFT};
+
+	if (led_id >= E1000_LED_NUM || led_id < 0) {
+		hw_dbg("igb ctl led id %d is invalid.", led_id);
+		return E1000_ERR_INVM_VALUE_NOT_FOUND;
+	}
+
+	ledctl = igb_rd32(hw, E1000_LEDCTL);
+	hw_dbg("igb ctl led id %d, mode %d, ledctl 0x%x.\n", led_id, mode, ledctl);
+
+	switch (mode) {
+	case LED_MODE_LINK_ON:
+		ledctl &= (~(E1000_LEDCTL_LED0_ALL << shift[led_id]));
+		ledctl |= (E1000_LEDCTL_LED0_LINKUP << shift[led_id]);
+		break;
+	case LED_MODE_ACT_BLINK:
+		ledctl &= (~(E1000_LEDCTL_LED0_ALL << shift[led_id]));
+		ledctl |= (E1000_LEDCTL_LED0_ACT << shift[led_id]);
+		break;
+	case LED_MODE_LINK_ON_ACT_BLINK:
+		ledctl &= (~(E1000_LEDCTL_LED0_ALL << shift[led_id]));
+		ledctl |= (E1000_LEDCTL_LED0_ACT_LINKUP << shift[led_id]);
+		break;
+	case LED_MODE_FORECE_OFF:
+		ledctl &= (~((E1000_LEDCTL_LED0_IVRT | E1000_LEDCTL_LED0_BLINK) << shift[led_id]));
+		ledctl |= (E1000_LEDCTL_MODE_LED_OFF << shift[led_id]);
+		break;
+	case LED_MODE_FORCE_ON:
+		ledctl &= (~(E1000_LEDCTL_LED0_BLINK << shift[led_id]));
+		ledctl |= ((E1000_LEDCTL_LED0_IVRT | E1000_LEDCTL_MODE_LED_ON) << shift[led_id]);
+		break;
+	case LED_MODE_FORECE_BLINK:
+		ledctl |= ((E1000_LEDCTL_LED0_BLINK | E1000_LEDCTL_MODE_LED_ON) << shift[led_id]);
+		break;
+	default:
+		return E1000_ERR_INVM_VALUE_NOT_FOUND;
+	}
+
+	wr32(E1000_LEDCTL, ledctl);
+
+	return 0;
+}
+EXPORT_SYMBOL(igb_ctrl_led_generic);
diff --git a/drivers/net/ethernet/intel/igb/e1000_led.h b/drivers/net/ethernet/intel/igb/e1000_led.h
new file mode 100644
index 000000000000..9160d13e9adc
--- /dev/null
+++ b/drivers/net/ethernet/intel/igb/e1000_led.h
@@ -0,0 +1,38 @@
+/* SPDX-License-Identifier: GPL-2.0 */
+/* Copyright (c) Huawei Technologies Co., Ltd. 2022-2022. All rights reserved. */
+
+#ifndef _E1000_LED_H_
+#define _E1000_LED_H_
+
+#include "e1000_hw.h"
+
+enum {
+	LED_MODE_LINK_ON = 0,           /* led on when port up; others off */
+	LED_MODE_ACT_BLINK = 1,         /* blink when activity; others off */
+	LED_MODE_LINK_ON_ACT_BLINK = 2, /* led on when port up; blink when activity */
+	LED_MODE_FORCE_ON = 3,
+	LED_MODE_FORECE_OFF = 4,
+	LED_MODE_FORECE_BLINK = 5,
+};
+
+/* LED Control */
+#define E1000_LEDCTL_LED0_MODE_SHIFT	0
+#define E1000_LEDCTL_LED0_BLINK		0x00000080
+#define E1000_LEDCTL_LED0_MODE_MASK	0x0000000F
+#define E1000_LEDCTL_LED0_IVRT		0x00000040
+
+#define E1000_LEDCTL_MODE_LED_ON        0xE
+#define E1000_LEDCTL_MODE_LED_OFF       0xF
+
+#define E1000_LEDCTL_LED1_MODE_SHIFT    8
+#define E1000_LEDCTL_LED2_MODE_SHIFT    16
+#define E1000_LEDCTL_LED0_ALL           0x000000FF
+#define E1000_LEDCTL_LED0_LINKUP        0x00000002
+#define E1000_LEDCTL_LED0_ACT           0x00000003
+#define E1000_LEDCTL_LED0_ACT_LINKUP    0x00000004
+
+#define E1000_LED_NUM 3
+
+s32 igb_ctrl_led_generic(struct e1000_hw *hw, s32 led_id, s32 mode);
+
+#endif
diff --git a/drivers/net/ethernet/intel/igb/e1000_nvm.c b/drivers/net/ethernet/intel/igb/e1000_nvm.c
index fa136e6e9328..0a1943e3e89e 100644
--- a/drivers/net/ethernet/intel/igb/e1000_nvm.c
+++ b/drivers/net/ethernet/intel/igb/e1000_nvm.c
@@ -780,3 +780,4 @@ void igb_get_fw_version(struct e1000_hw *hw, struct e1000_fw_version *fw_vers)
 			| eeprom_verl;
 	}
 }
+EXPORT_SYMBOL(igb_get_fw_version);
diff --git a/drivers/net/ethernet/intel/igb/e1000_phy.c b/drivers/net/ethernet/intel/igb/e1000_phy.c
index 34138abec8dd..dd8ea82ce1b3 100644
--- a/drivers/net/ethernet/intel/igb/e1000_phy.c
+++ b/drivers/net/ethernet/intel/igb/e1000_phy.c
@@ -78,6 +78,7 @@ s32 igb_get_phy_id(struct e1000_hw *hw)
 out:
 	return ret_val;
 }
+EXPORT_SYMBOL(igb_get_phy_id);
 
 /**
  *  igb_phy_reset_dsp - Reset PHY DSP
diff --git a/drivers/net/ethernet/intel/igb/igb_main.c b/drivers/net/ethernet/intel/igb/igb_main.c
index 7f12a19852b6..8c97181f168b 100644
--- a/drivers/net/ethernet/intel/igb/igb_main.c
+++ b/drivers/net/ethernet/intel/igb/igb_main.c
@@ -6671,9 +6671,12 @@ void igb_update_stats(struct igb_adapter *adapter)
 	adapter->stats.ruc += rd32(E1000_RUC);
 	adapter->stats.rfc += rd32(E1000_RFC);
 	adapter->stats.rjc += rd32(E1000_RJC);
+	adapter->stats.torl += rd32(E1000_TORL);
 	adapter->stats.tor += rd32(E1000_TORH);
+	adapter->stats.totl += rd32(E1000_TOTL);
 	adapter->stats.tot += rd32(E1000_TOTH);
 	adapter->stats.tpr += rd32(E1000_TPR);
+	adapter->stats.lenerrs += rd32(E1000_LENERRS);
 
 	adapter->stats.ptc64 += rd32(E1000_PTC64);
 	adapter->stats.ptc127 += rd32(E1000_PTC127);
@@ -6755,6 +6758,7 @@ void igb_update_stats(struct igb_adapter *adapter)
 		adapter->stats.b2ogprc += rd32(E1000_B2OGPRC);
 	}
 }
+EXPORT_SYMBOL(igb_update_stats);
 
 static void igb_tsync_interrupt(struct igb_adapter *adapter)
 {
-- 
2.17.1

